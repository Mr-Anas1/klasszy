{
  "name": "lms-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:abc": "set NEXT_PUBLIC_SCHOOL_CONFIG=abc-school&& set SCHOOL_CONFIG=abc-school&& next build",
    "build:xyz": "set NEXT_PUBLIC_SCHOOL_CONFIG=xyz-school&& set SCHOOL_CONFIG=xyz-school&& next build",
    "start": "next start",
    "lint": "eslint",
    "cap:build": "npm run build",
    "cap:build:abc": "npm run build:abc",
    "cap:build:xyz": "npm run build:xyz",
    "cap:sync": "npm run cap:build && npx cap sync",
    "cap:sync:abc": "npm run cap:build:abc && npx cap sync",
    "cap:sync:xyz": "npm run cap:build:xyz && npx cap sync",
    "cap:run:android": "npm run cap:sync && npx cap run android",
    "cap:run:android:abc": "npm run cap:sync:abc && npx cap run android",
    "cap:run:android:xyz": "npm run cap:sync:xyz && npx cap run android",
    "cap:run:ios": "npm run cap:sync && npx cap run ios",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "school:dev": "node scripts/run-school.mjs dev",
    "school:build": "node scripts/run-school.mjs build",
    "school:sync": "node scripts/run-school.mjs sync",
    "school:android": "node scripts/run-school.mjs android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2",
    "firebase": "^12.12.1",
    "lucide-react": "^1.14.0",
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
